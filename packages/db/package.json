{
  "name": "@stream-share/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "db:migrate": "dotenvx run -q -f ../../.env.development -- drizzle-kit migrate",
    "db:generate": "dotenvx run -q -f ../../.env.development -- drizzle-kit generate",
    "db:studio": "dotenvx run -q -f ../../.env.development -- drizzle-kit studio --port 7000",
    "db:push": "dotenvx run -q -f ../../.env.development -- drizzle-kit push"
  },
  "dependencies": {
    "drizzle-orm": "1.0.0-beta.22",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "^2.22.0",
    "@types/pg": "^8.20.0",
    "drizzle-kit": "1.0.0-beta.22",
    "typescript": "^7.0.2"
  }
}
