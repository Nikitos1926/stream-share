# Copy to infra/.env.production on the host (chmod 600). Read only by
# docker compose interpolation. LF line endings, no quotes, no inline comments.

REGISTRY=ghcr.io/nikitos1926/stream-share
IMAGE_TAG=latest

POSTGRES_USER=streamshare
POSTGRES_PASSWORD=
POSTGRES_DB=streamshare

AUTH_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

PUBLIC_HOST=streamshare.space
ACME_EMAIL=nikita.kabakov.mail@gmail.com

MEDIASOUP_ANNOUNCED_IP=
MEDIASOUP_NUM_WORKERS=2
