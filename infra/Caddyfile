{
    email {$ACME_EMAIL}
}

{$PUBLIC_HOST} {
    encode zstd gzip

    # signaling owns these paths; everything else is Next
    @signaling path /streams /streams/* /ws/streams/*
    handle @signaling {
        reverse_proxy signaling:4000
    }

    handle {
        reverse_proxy web:3000
    }
}
